p#notice = notice

h1 = @article.title
#article
script document.getElementById('article').innerHTML = marked(`#{@article.article}`);

- if current_user.id == @article.user_id
  => link_to '編集', edit_article_path(@article)
  '|
=< link_to '戻る', articles_path
